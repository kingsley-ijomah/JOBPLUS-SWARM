name: Build and Push

on:
  workflow_run:
    workflows: ["Deploy Code"]
    branches: [experimental]
    types:
      - completed

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Testing
        run: |
          echo "Running next workflow step..."
