name: Build and Push

on:
  workflow_dispatch:

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Install Vault CLI
        run: |
          echo "Running next workflow step..."
